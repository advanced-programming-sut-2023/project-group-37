package connection.packet;

public enum PacketType {
    REGISTER_PACKET,
    LOGIN_PACKET,
    LOGOUT_PACKET,
    ALIVE_PACKET,
    TILES_PACKET,
    POPUP_PACKET,
    USER_PACKET,
    FRIEND_REQUEST_PACKET,
    CHAT_PACKET
}
